<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.chandigarhthecitybeautiful.ui.main.MainFragment"
        android:label="main_fragment"
        tools:layout="@layout/main_fragment" >
        <action
            android:id="@+id/action_mainFragment_to_placeDetails"
            app:destination="@id/placeDetails" />
    </fragment>
    <fragment
        android:id="@+id/placeDetails"
        android:name="com.chandigarhthecitybeautiful.PlaceDetails"
        android:label="place_details_fragment"
        tools:layout="@layout/place_details_fragment" >
        <argument android:name="PlaceId"
            app:argType="integer" />
    </fragment>
</navigation>